# Roadmap

A phased, small-task plan. Each task is approval-sized, ships a working slice, and ends with verification. Phases are ordered for value, not gated — within a phase, tasks may run in parallel where the surfaces are disjoint.

## Phase A — Foundations

- **A1 — Repo, tooling, CI skeleton** — *complete*
  - Clean-architecture .NET 8 solution boots, builds, has Swagger, health checks, Serilog, ProblemDetails, rate limiting, CORS, `IFileStorage` local-disk impl.
  - Vite + React + TS strict + Tailwind + shadcn foundation + ESLint + Prettier + i18next (en/ar with RTL).
  - GitHub Actions CI runs build/test/lint on PRs.
  - Five docs.
- **A2 — Design tokens & shadcn primitives (RTL- and age-aware)** — *complete*
  - Junior / Explorer / Staff theme variants on shared components.
  - 9 tokenised primitives + `useTheme()` hook + AGENTS.md.
  - `/design-system` showcase covers all primitives × LTR/RTL × all themes.
  - Local ESLint rule banning directional Tailwind classes in `src/`.
- **A3 — Application shells, layout primitives, navigation, OpenAPI types** — *complete*
  - Three layout-route shells: `MarketingShell` (`/`), `StudentShell` (`/student/*`), `StaffShell` (`/staff/*`). Parent shell deferred — see frontend-architecture.md.
  - Layout primitives: `Container`, `PageTitle`, `PageHeader`, `PageSection`, `SectionDivider`, `PageActions`, `EmptyState`, `Breadcrumbs`.
  - Typed nav config + role-aware filtering helpers; chrome components (`MarketingHeader/Footer`, `StudentTopBar`, `StaffSidebar`, `StaffTopBar`, `MobileNavSheet`).
  - One placeholder page per shell.
  - OpenAPI → TS pipeline (`npm run gen:api-types` → `src/types/api.d.ts`); no client code generated.
  - Git initialised with first commit.
- **A4 — Backend supplemental & integration test harness** *(deferred)*
  - Polished integration-test harness for SQL Server (Testcontainers or LocalDB) — currently empty DbContext + in-memory tests.
  - Serilog destructuring policies for PII redaction.
  - CI: dependency vulnerability scan (`dotnet list package --vulnerable`, `npm audit --omit=dev`).

## Phase B — Identity & marketing

- **B1 — Marketing shell + Home page**
- **B2 — Parent registration + login** (Identity, JWT + refresh, email verification, password reset, rate-limited auth endpoints)
- **B3 — Child profile + child PIN login** (parent-scoped child credentials, scoped JWT claims)

## Phase C — Course catalog

**Backend catalog is complete** (domain → public read API → full admin write API → contract sync). UI is the next phase.

- **C1B — Backend course domain foundation** — *complete*
  - Course aggregate (modules, instructors, outcomes), Category, LearningPath/Item; `LocalizedText`/`CoursePricing`/`CourseMedia` VOs; EF mappings; `InitialCourseDomain` migration; dev seed data; domain + infra tests.
- **C1D — Public catalog read API** — *complete*
  - `GET /api/v1/catalog/{courses,courses/{slug},categories,learning-paths,learning-paths/{slug}}` — localized (en/ar), paginated, filterable, published-only.
- **C1C — Admin catalog write API** — *complete*
  - **C1C.1** admin course read scaffolding + `Courses.*` policies; **C1C.2** course create/update (+ `RowVersion` concurrency, audit); **C1C.3** course lifecycle (publish/unpublish/archive/restore + coded publish-readiness); **C1C.4** course modules + instructor assignment (Identity-validated); **C1C.5A** categories CRUD + activate/deactivate; **C1C.5B** learning-paths CRUD + lifecycle + readiness; **C1C.5C** learning-path items (add/remove/reorder).
  - All behind `Courses.* / Categories.Manage / LearningPaths.*` policies (Admin/SuperAdmin). Optimistic concurrency via `RowVersion` on Course/Category/LearningPath. 288 backend tests green.
- **C1C.6 — Backend hardening & OpenAPI type generation** — *complete*
  - All 36 catalog/admin operations verified in Swagger; frontend API types regenerated (`src/types/api.d.ts`, git-ignored); docs updated; pending prod migrations noted.
- **C2 — Frontend catalog & admin catalog UI** *(in progress)* — UI only; the API contract is frozen above.
  - **Admin course authoring — complete (C2A–C2K).** Courses list + filters (C2D), create page (C2J), and the full course editor — overview/content update (C2F), lifecycle publish/unpublish/archive/restore (C2G), modules management (C2H), instructor assignment (C2I) — with `RowVersion` concurrency, publish-readiness integration, en/ar + full RTL, and a blue-modern visual direction unified across list → create → editor. C2K polished cross-screen consistency and added a UI-level authoring-flow E2E smoke. Backend-connected authoring E2E (real create → publish) is a tracked follow-up — see [testing.md](testing.md).
  - **Admin categories UI — complete (C3A).** `/staff/categories` list (search + active/sort filters, pagination, desktop table + mobile cards), create/edit side-dialog (RHF + Zod, edit fetches detail for descriptions + fresh `rowVersion`), and activate/deactivate confirmation — with 409 concurrency (parametrised `ConcurrencyAlert`) and slug-conflict handling, en/ar + full RTL, and the shared blue-modern direction. Dev `?state=` fixtures cover every state for visual QA.
  - **Admin learning-paths list UI — complete (C3B).** `/staff/learning-paths` list only: URL-bound search + status/age-band/visibility filters + sort, pagination, desktop table + mobile cards, publish-state/age-band/visibility badges reused from courses, course-count (`itemCount`), and a Manage link to the existing `/staff/learning-paths/:id` placeholder. "New learning path" is a disabled "Soon" action (create is a later task). en/ar + full RTL; dev `?state=` fixtures + an E2E smoke check. *Note: the list DTO has no category/difficulty, so those columns are intentionally omitted.*
  - **Admin learning-path detail foundation — complete (C3C).** `/staff/learning-paths/:id` read-only tabbed shell (Overview / Content / Items / Publishing) with a `?tab=` deep-link, gradient hero, reused status/age/visibility badges, bilingual read-only content, a read-only items (courses) preview + empty state, and a Publishing tab with the unmet-only readiness checklist (`learningPaths.readiness.*` messageKeys) plus **disabled** lifecycle buttons + a coming-soon note. Distinct loading/error/not-found/**forbidden** (403) states. en/ar + full RTL; dev `?state=` fixtures + an E2E smoke check. *Note: the detail DTO exposes summary but no description, and items carry no required/optional/duration/category — rendered accordingly, not faked.*
  - **Admin learning-path update form — complete (C3D).** The C3C detail shell's Overview + Content tabs are now RHF + Zod forms (`useUpdateLearningPath`): editable slug/age band/visibility/media + bilingual title/summary, gradient-hero save/cancel with a dirty chip and a saved toast. `rowVersion` concurrency (409 → shared `ConcurrencyAlert` with LP copy + reload), slug-clash 409 → slug field, 400 → per-field mapping. State-aware locking mirrors the backend: **archived → read-only**, **slug editable only while Draft**, **listing only while Published** (each with a hint), so the UI never trips an invalid-state 400. Items + Publishing stay read-only. en/ar + full RTL; dev `?state=` fixtures + an E2E smoke check.
  - **Admin learning-path items management — complete (C3E).** The Items tab is now functional (`useAddLearningPathItem` / `useRemoveLearningPathItem` / `useReorderLearningPathItems`, mirroring the C2H course modules manager): add a course by id (RHF + Zod dialog), remove via an alert-dialog, and reorder with move up/down (no drag-and-drop). Every mutation carries the path `rowVersion` and returns the full detail, so the list **and the Publishing readiness update together**. Error handling: 409 concurrency → shared `ConcurrencyAlert` + reload; 409 duplicate → "already in the learning path" on the courseId field; 404 → "no course found"; 400 → per-field. Archived paths disable add/remove/reorder. en/ar + full RTL; dev `?state=` fixtures + an E2E smoke check. *Note: there is no update-item endpoint, so notes are set at add-time only (no edit-note); item cards show course title (English) / slug / id, publish state, and note.*
  - **Admin learning-path lifecycle actions — complete (C3F).** The Publishing tab is now functional (`usePublishLearningPath` / `useUnpublishLearningPath` / `useArchiveLearningPath` / `useRestoreLearningPath`, mirroring the C2G course lifecycle panel): state-aware Publish / Unpublish / Archive / Restore with confirmation dialogs, a calm-destructive treatment for the high-impact Unpublish/Archive, inline success feedback, and a disabled-with-reason Publish on a not-ready Draft. Every action sends the current `rowVersion`; the lifecycle response is partial so the hooks invalidate the detail (server is source of truth — no optimistic state, no auto-retry). Error handling: 422 publish-readiness → a "Publish blocked" alert above the standing unmet-only checklist (`readinessFromError`); 409 concurrency → shared `ConcurrencyAlert` (LP copy) + reload; 400/403/404/generic → safe mapped messages. Archived paths stay editor/items-locked (C3D/C3E) and only offer Restore. en/ar + full RTL; dev `?state=` lifecycle fixtures (publishable/publishConfirm/publishBlocked/unpublishConfirm/archiveConfirm/restoreConfirm/lifecycleConflict/publishSuccess) + two E2E smoke checks.
  - **Admin learning-path create page — complete (C3G).** `/staff/learning-paths/new` is a real create page (mirroring the C2J course create page), replacing the disabled "Soon" action + the placeholder route. It reuses the C3D `learningPathFormSchema` / `LearningPathFormValues` / fields for a short guided "draft first" step — Basics (title en/ar, optional slug, age band) + Content (bilingual summary) — with a side "after you create" guidance card. Submits via `useCreateLearningPath` and redirects to the editor at `/staff/learning-paths/:id` using the returned `id`; media/items/publishing are completed there. Error handling: 409 duplicate slug → slug field + summary; 400 → per-field via `mapServerErrors`; generic fallback; an unlikely missing-id response is guarded rather than losing the draft. `isListed` and media are intentionally omitted from create (a new path is always an unlisted Draft). en/ar + full RTL; dev `?state=` fixtures (filled/invalid/conflict/submitting) + two E2E smoke checks. **The admin learning-path authoring loop is now complete: create draft → update content → add items → publish.**
  - **Admin learning-path authoring polish & QA — complete (C3H).** A full polish/verification pass over the complete authoring loop (list → create → editor overview/content → items → readiness → publish/unpublish/archive/restore → back to list/detail). The flow was reviewed screen-by-screen against the course-authoring benchmark across LTR/RTL/desktop/tablet/mobile and found consistent; the one fix was removing a redundant per-tab "archived, read-only" banner from the Items tab (the editor already shows a single global archived notice on every tab — now matching the course Modules tab, and controls stay locked). Added a consolidated E2E authoring-flow smoke (list → click New → create page → editor tab navigation to Courses + Publishing). en/ar parity retained. **The admin learning-path authoring loop is verified end-to-end at benchmark quality.**
  - **Public catalog browse foundation — complete (C4A).** The first public-facing slice, under the `MarketingShell` at `/catalog`, `/catalog/courses`, and `/catalog/learning-paths` (the marketing nav's Courses/Paths links are now live). A new unauthenticated API layer (`lib/api/catalog.ts`) + read-only React Query hooks (`usePublicCourses` / `usePublicLearningPaths` / `usePublicCategories`) wrap the `/api/v1/catalog` endpoints; responses are already localized server-side, so the active language is part of the query key. A joyful-but-premium blue hero, category-chip + age-band + sort filters and search for courses (all URL-bound), and a simpler age-band filter for learning paths (the LP endpoint supports only `ageBand`/`page` — no search/category/sort). Reusable `PublicCourseCard` / `PublicLearningPathCard` (fully clickable via a stretched CTA link), polished loading skeletons, friendly empty (filtered vs coming-soon) and error/retry states. Detail routes (`/catalog/*/:slug`) are safe "coming soon" placeholders until C4B. Card media is an intentional gradient+icon tile (the DTO exposes a `thumbnailKey` storage key, not a resolvable URL — see Findings). en/ar parity + full RTL; dev `?state=` fixtures, helper + component tests, five E2E smoke checks, and visual-QA matrix rows.
  - **Public course detail page — complete (C4B).** `/catalog/courses/:slug` is now a real read-only detail page (via `usePublicCourse`, localized server-side), replacing the placeholder. A hero (back link, category chip, title/subtitle/summary, age-band/difficulty/format/ages badges, optional "Watch intro" when `promoVideoUrl` is present) over a gradient media tile; a main column that renders only the sections with real data — **About** (`description`), **What your child will learn** (`outcomes`), and **Course modules** (`modulesPreview`, ordered); and a sidebar with a **Course details** list (category, ages, level, format, price, available languages) plus a calm **"Enrollment coming soon"** card (a disabled CTA — no checkout/payment). Polished loading skeleton, friendly **not-found** (404), and error/retry states. en/ar parity + full RTL; dev `?state=` fixtures (rich/minimal/noModules/free/paid/error/notfound/loading), helper + component tests, E2E smoke checks, and visual-QA matrix rows. *Findings: the public course DTO exposes instructors as opaque `instructorUserId` + `role` only (no names), and has no lesson count / duration / prerequisites / related paths / dates — those sections are intentionally omitted, not faked. Media is still a gradient tile (thumbnail/hero are storage keys, not URLs).*
  - **Public learning-path detail page — complete (C4C).** `/catalog/learning-paths/:slug` is now a real read-only detail page (via `usePublicLearningPath`, localized server-side), replacing the last catalog placeholder (the placeholder module is removed). A hero (back link, "Learning path" chip, title/summary, age-band + course-count badges) over a gradient media tile; the core is an **ordered course sequence** rendered as a numbered `<ol>` reusing `PublicCourseCard` (each links to `/catalog/courses/:slug`), with a friendly "courses coming soon" empty state; and a sidebar with a **Path details** list (age group, course count) plus a calm **"Path access coming soon"** card (a disabled CTA — no checkout/payment/progress). Polished loading skeleton, **not-found** (404), and error/retry states. en/ar parity + full RTL; dev `?state=` fixtures (rich/minimal/emptyCourses/error/notfound/loading), helper + component tests, E2E smoke checks, and visual-QA matrix rows. *Findings: the LP detail DTO carries no description/outcomes/dates and the member courses are full `CourseCardDto`s (so the browse card is reused directly).* **The public catalog browse + detail set (landing, courses/paths browse, course/path detail) is now complete.**
  - **Public catalog polish & homepage integration — complete (C4D).** A coherence/discoverability pass making the finished browse + detail set feel production-ready from the marketing surface. The marketing homepage is no longer an A3 placeholder: the hero CTAs route into the catalog (`/catalog`, `/catalog/courses`), a "Why families choose Code Spark" highlights panel replaces the old internal-surface links (the public homepage no longer promotes `/student`, `/staff`, or `/design-system`), and a new "Explore our learning catalog" section presents Courses vs Learning paths with a "See the full catalog" link. The marketing footer's Product group now links **Courses**/**Learning paths** to the live browse pages (not-yet-built company/legal entries stay as plain labels). A shared `CatalogNavCard` (`components/catalog/`) is reused by the landing and homepage so both promote the catalog identically; the duplicated browse pager was extracted to a shared `CatalogPagination`; and the learning-paths browse now explains why it has fewer filters (the LP API only supports `ageBand`). en/ar parity + full RTL retained; new homepage/footer/pagination component tests, a homepage→catalog E2E smoke check (asserting no internal-surface/checkout links), and the existing visual-QA matrix (which already covers `/` + all catalog routes across en/ar/desktop/tablet/mobile). *Media finding: no media-URL resolver exists — `IFileStorage` reads/writes disk streams (no public URL), there is no media controller serving files by key, and no CDN base env var. `thumbnailKey`/`heroKey` remain storage keys, so the gradient media tile stays; no URLs are faked.*
  - **Public media resolver / image-rendering foundation — complete (C4E).** A single, safe media strategy for the public catalog. A pure `resolveCatalogMediaUrl(key)` (`lib/media/`) turns a storage key into a URL only when it can do so safely: empty → `null`; an already-absolute `https://` URL → passthrough; any other scheme, disk path (`C:\…`, `file:`), or protocol-relative `//host` → `null`; a relative key → joined against an operator-configured `VITE_MEDIA_BASE_URL` (encoded, no double slash) or `null` when unset. A shared `CatalogImage` (`components/catalog/`) consumes it across the cards (`CatalogCardMedia`) and both detail heroes: it renders a lazy `<img>` (eager for heroes) with alt text (`thumbnailAlt` → localized `catalog.media.*Alt` fallback) and, on load error, swaps to the fallback — so a broken URL never shows a broken-image icon. The fallback itself is now an intentional, on-brand **Code Spark Kids blue** cover (deep-blue gradient + cyan/yellow glow + coding icon) via a `.catalog-media-fallback` class + brand values in `styles/index.css` (no hex in components). en/ar parity + full RTL; resolver + `CatalogImage` unit tests (safe/unsafe keys, image vs fallback, alt fallback, error→fallback), two E2E media smoke checks (branded tile present, no broken `<img>`), and the existing visual-QA matrix. *Status: real images are **not** rendered yet because no public media host exists — the resolver returns `null` for storage keys until `VITE_MEDIA_BASE_URL` points at a real origin (a backend public file endpoint or CDN). No URLs are faked and no disk paths are exposed. Lighting up real imagery needs **C4F Option C — a backend public media endpoint / CDN URL contract** (serve keys publicly, or add resolved URL fields to the public DTOs).*
  - **Public catalog media endpoint — complete (C4F).** The backend now serves catalog media so real images can render. A public, read-only, **image-only** endpoint `GET /api/v1/media/{**key}` (`MediaController`) streams a file from the configured storage root by opaque key. Security is layered: a pure, exhaustively-tested `CatalogMediaKey` validator (Application) rejects empty/oversized (>256) keys, control/NUL chars, `..` traversal, backslashes, `:` (drive/scheme), rooted `/`, protocol-relative `//`, empty segments, bare-extension dotfiles, and any non-allow-listed extension (jpg/jpeg/png/webp/gif — **SVG excluded**); then `LocalDiskCatalogMediaStore` (Infrastructure) re-checks canonical path containment under the root before opening the stream. Every invalid/unsupported/missing key returns **404** (no 400-vs-404 probing signal, no path/stack leakage); valid images return `200` with the validated `Content-Type`, `X-Content-Type-Options: nosniff`, and `Cache-Control: public, max-age=3600`. Backend tests: 30 validator unit cases + 6 endpoint integration tests (valid png/jpeg, missing→404, unsupported/SVG→404, encoded-traversal never serves an outside file, no path leakage). Frontend: the C4E resolver gained a same-origin **root-relative base** mode (`/api/v1/media`) alongside the absolute dev origin; `.env.example` documents both; no other frontend changes. *DTOs intentionally keep `thumbnailKey`/`heroKey` (no `thumbnailUrl` fields) — the frontend resolver already turns keys into URLs, so the OpenAPI surface stays unchanged and `api.d.ts` needs no regeneration. Real images still require (a) actual files uploaded under the storage root and (b) `VITE_MEDIA_BASE_URL` set; there is no upload pipeline yet and the dev seed ships no files, so local screenshots remain fallback-only. No fake URLs, no disk-path exposure.*
  - **Backend-connected media evidence — complete (C4G).** Proves the full media flow (DTO key → `resolveCatalogMediaUrl` → `GET /api/v1/media/{**key}` → real `<img>`) end-to-end in a real browser, closing the C4F "screenshots remain fallback-only" gap. A self-contained orchestrator (`frontend/scripts/e2e-media-smoke.mjs`, `npm run test:e2e:media`) generates deterministic, tiny **Code-Spark-blue PNG** fixtures locally (no committed binaries, no SVG, no hidden metadata) into a private temp storage root, starts the **real API** pointed at that root with catalog DB seeding **off** (the media endpoint is filesystem-only, so this needs **no SQL Server**), starts Vite with `VITE_MEDIA_BASE_URL` at the endpoint, then drives headless Chromium across the course/path browse cards + detail heroes asserting a real `<img>` loads from `/api/v1/media/` (`naturalWidth`/`naturalHeight > 0`, `src` starts with the configured base) and that the branded fallback is **not** used for that exact fixture tile — while unkeyed tiles still show the fallback and no `<img>` renders broken. It captures real-image Visual QA screenshots (course/path card + detail hero, desktop LTR + mobile RTL) into the git-ignored artifacts dir, then tears everything down and deletes the temp root. The public browse fixtures gained matching keys (`python-adventures` → `courses/python/thumb.png`, `junior-coder-journey` → `paths/junior/thumb.png`) so card-level real images are provable too; these are inert (branded fallback) until a media host is configured, so the frontend-only suites (377 unit, 35 E2E incl. the C4E fallback check) stay green. **Backend-connected public media is now proven.** *No product surface changed; no C4F security validation was weakened; no fake URLs, prod domains, or disk paths were introduced.*
  - **Catalog media admin upload / preview — complete (C4H).** Staff can now *produce* the media the public catalog already serves. A new authenticated, staff-only endpoint `POST /api/v1/admin/catalog/media` (`AdminCatalogMediaController`, `CoursePolicies.Manage` → Admin/SuperAdmin) accepts one image over multipart/form-data and returns a server-generated storage key — never a URL or disk path. Security is layered: the file type is decided by **sniffing magic bytes** (`CatalogMediaUpload`), never by trusting the filename or declared `Content-Type`, so SVG/HTML/scripts, double-extension tricks, and spoofed MIME types are rejected (only PNG/JPEG/WebP/GIF pass, via the same `CatalogMediaKey` allow-list the read path uses); size is capped at 5 MB (friendly 400 + a `RequestSizeLimit` backstop); the key is generated server-side (`catalog/{courses|learning-paths}/{safe-slug}/{thumbnail|hero}/{guid}.{ext}`) and re-validated with `CatalogMediaKey`; and the write goes through a new `ICatalogMediaWriteStore` on `LocalDiskCatalogMediaStore` that enforces the same canonical-containment check as the read path. The frontend adds a multipart `postForm` client + `uploadCatalogMedia`/`useUploadCatalogMedia`, and an additive **`MediaUploadField`** (preview via the shared `CatalogImage`/resolver, accessible file input, uploading state, client pre-validation, safe localized errors, en/ar + full RTL) wired into the course editor (thumbnail + hero) and the learning-path editor (thumbnail) — above, not replacing, the existing key/alt fields, so manual edit/clear still work; the returned key is written into the form and persisted with the normal save. Tests: 47 upload-policy unit cases (sniff accept/reject incl. SVG, size, kind, safe-key generation) + 10 endpoint integration tests (admin uploads a PNG that is then servable via `/api/v1/media`; unauth 401; parent/instructor 403; SVG/text-as-png/oversize/unknown-kind/missing → 400; no path leakage) + 7 `MediaUploadField` component tests; the media orchestrator gained two staff-themed admin-editor real-preview scenarios (desktop LTR + mobile RTL). *No public upload endpoint, no media-management UI, no CDN; SVG never served; C4F read-path security untouched. `heroKey` upload for learning paths and a pre-entity create-page upload are intentionally deferred.*
  - **Authenticated media upload E2E evidence — complete (C4I).** Closes the C4H "no real browser upload round-trip" gap by driving the whole authenticated flow against a **real** backend. A self-contained orchestrator (`frontend/scripts/e2e-media-upload-smoke.mjs`, `npm run test:e2e:media-upload`) spawns a real Kestrel API on a throwaway **LocalDB** database (catalog seeded + a **dev-only, opt-in** `Dev:SeedStaffAdmin` admin — password required via config, Development-only, never in prod) with an isolated temp storage root and CORS for the Vite origin, then in a real browser: **UI logs in** as the seeded Admin (real JWT), opens the real course editor (DB-backed), uploads a tiny PNG through the actual `MediaUploadField` file input → `POST /api/v1/admin/catalog/media` (real Bearer) → asserts the returned key is written into the form and the preview `<img>` loads (`naturalWidth > 0`, `src` on `/api/v1/media/`, no fallback, **no disk path** anywhere) — captured desktop-LTR **and** mobile-RTL. This also fixed a real `CatalogImage` bug the flow exposed: a previously-broken image (stale key → missing file) never recovered when the key was replaced, because `failed` state didn't reset on `src` change — so uploading over a broken image kept showing the fallback (fixed + regression-tested). ***Strategy B (honest):*** the editor **Save + public-detail** preview are **deferred, not faked** — the real-SQL-Server run surfaced a **pre-existing bug in the catalog course-update path** (unrelated to media): rewriting a course's owned outcomes collection throws a spurious `DbUpdateConcurrencyException`, so every editor Save `409`s even when the rowVersion matches (`currentRowVersion === sent`). It is invisible to the C2 suite (which runs on SQLite, where the rowversion concurrency token is demoted) and needs a dedicated backend fix. *No committed screenshots/uploads/api.d.ts; C4F/C4H upload security untouched; no fake URLs or path leakage.*
  - **SQL Server catalog-update concurrency fix + full upload E2E — complete (C4J).** Fixed the pre-existing, production-blocking SQL-Server-only bug C4I surfaced: updating a course that rewrites its owned `CourseOutcomes` collection threw a spurious `DbUpdateConcurrencyException` (→ 409) even when the submitted `rowVersion` was current. **Root cause** — EF Core's orphan detection mis-tracks a wholesale owned-collection replace on SQL Server's native `rowversion` as a *Modified* child, so its UPDATE matches 0 rows and SaveChanges reports a false conflict; SQLite never saw it because `AppDbContext` demotes the token to a no-op there (which is also why the C2 suite missed it). **Fix** (`AdminCourseService.UpdateAsync`): delete the old outcome rows and insert the new ones explicitly (mirroring the module path's `db.Add`) instead of relying on orphan detection, and drop the now-redundant `RowVersion.OriginalValue` override — concurrency stays enforced by the explicit `EnsureRowVersionMatches` check **plus** EF's loaded original value driving the native token, so stale writes still `409`. Learning-path update was investigated and does **not** share the bug (its update rewrites no child collections). New **SQL Server / LocalDB** integration coverage (`SqlServerCatalogUpdateConcurrencyTests`, auto-skipped when LocalDB is absent) reproduces the 409 **before** the fix and, **after** it, proves current-`rowVersion` saves (outcomes + media persist, fresh token), stale-`rowVersion` → 409, two-client no-lost-update, and learning-path parity — the gap the SQLite suite structurally could not catch. With the bug fixed, the C4I upload E2E is promoted to **Option A (full chain):** login → editor → upload → preview → **Save (200)** → **public course detail renders the uploaded hero** (`src` on `/api/v1/media/…`, `naturalWidth > 0`, no fallback, no disk-path leak), desktop-LTR **and** mobile-RTL. Backend **386** tests green; frontend **385** unit + **35** E2E + **12** media E2E + the full media-upload E2E green. *No migrations, no model change; C4F/C4H media security untouched; concurrency protection not weakened; no committed screenshots/uploads/api.d.ts; no fake URLs or path leakage.*
  - **Catalog media production-readiness hardening — complete (C4K).** Makes the media storage story deployment-safe and operator-clear without building a cloud provider (LocalDisk stays the only implementation). A dedicated operator doc, [`docs/release-readiness/CATALOG_MEDIA_STORAGE_PRODUCTION_READINESS.md`](release-readiness/CATALOG_MEDIA_STORAGE_PRODUCTION_READINESS.md), documents the current model, backend (`FileStorage__LocalDisk__RootPath`) + frontend (`VITE_MEDIA_BASE_URL`) config with dev/same-origin/CDN examples, allowed types (PNG/JPEG/WebP/GIF — **SVG never served**), the 5 MB limit, the layered security guarantees (twice-validated keys, canonical containment, no path exposure, auth-only writes, `nosniff`), cache behavior (immutable GUID keys → CDN-cacheable; replace = new key = free invalidation), **orphan/cleanup** (deferred by design — a future job must verify DB references before deleting, never delete by a request-supplied path), backup/restore (media volume + DB together), and deployment/rollback checklists + known limitations. The one code change is a **readiness signal**: a `CatalogMediaStorageHealthCheck` on **`/health/ready`** ensures the media root exists (creating it) and is writable via a transient probe file, reporting **Degraded — never Unhealthy** (so a misconfigured/read-only root is observable without dropping the instance or stopping public reads) and **logging the path, never returning it**; `/health/live` is now dependency-free (process liveness only). Backend **+4** tests (health check Healthy/Degraded-no-leak + `/health/ready`+`/health/live` wiring). Media read + full upload E2E re-run green (no regression). *No cloud provider, no fake CDN URLs, no hardcoded prod domain in runtime code; no migrations; upload/read/upload-concurrency (C4F/C4H/C4J) security unchanged.*
  - **Catalog media orphan cleanup — complete (C4L).** Closes the C4K operational gap (orphaned files accumulating when media is replaced or an upload is never saved) with a deliberately conservative, admin-only capability — **nothing runs automatically** (no startup hook, no schedule). A new maintenance surface (`ICatalogMediaMaintenanceStore`, implemented by `LocalDiskCatalogMediaStore` alongside the read/write splits) confines **enumeration and deletion to the `catalog/` prefix**, re-validating every key with `CatalogMediaKey` + canonical containment before touching anything. `CatalogMediaCleanupService` cross-checks each file against **every persisted media reference** — course + learning-path thumbnail/hero keys, **including soft-deleted rows** (restorable → protected), blanks ignored, duplicates deduplicated, case-insensitive (over-protective) — and reports orphan candidates only when the file is also **older than the grace period** (default 24 h, request range 1–8760 h validated → 400, and clamped again in the service as defence in depth). `POST /api/v1/admin/catalog/media/cleanup` (`CoursePolicies.Manage`, Admin/SuperAdmin) is **dry-run by default**: deletion requires an explicit `"dryRun": false`, goes file-by-file through the maintenance store, and logs every deleted key. The response carries **safe relative keys and counts only** (candidates + too-young + invalid-keys-under-catalog + **missing referenced files** reported separately, never deletable; lists capped at 200 with exact counts) — no disk paths. **+13 backend tests** (deterministic seeded catalog on a temp root: dry-run reports-but-never-deletes, delete-mode removes only the eligible orphan, soft-deleted/duplicated/young/invalid/outside-prefix files all survive, custom grace honoured, out-of-range → 400, anonymous 401 / parent 403, no-path-leak; store-level refusals for traversal/non-catalog/non-image keys). Readiness doc §11 rewritten as the operator runbook (endpoint, rules, example response, cadence, rollback). *No UI (none needed), no scheduled deletion, no provider work; C4F/C4H/C4J/C4K behavior untouched; no migrations.*
  - **Public catalog production readiness gate — complete (C4M).** A holistic go/no-go QA gate over the whole public catalog + media chain (C4A–C4L), producing [`docs/release-readiness/C4M_PUBLIC_CATALOG_PRODUCTION_READINESS_GATE.md`](release-readiness/C4M_PUBLIC_CATALOG_PRODUCTION_READINESS_GATE.md). Reviewed and re-verified in one run: route/state inventory for all 5 public routes + admin authoring (honest disabled CTAs, no internal links on public pages, unknown routes → home), the full media pipeline (serve/upload/preview/save/public render/health/cleanup), auth policies (401/403 paths), SQL Server concurrency (LocalDB suite ran, not skipped), and docs/operator readiness. Verification: backend **402/402**, frontend **385** unit + lint/format/typecheck/build clean, E2E **35/35**, media E2E **12/12**, media-upload E2E **full chain green**. Evidence: **60 git-ignored screenshots** (41 fresh public-matrix shots across en/ar × desktop/tablet/mobile + the 19 real-backend media/upload shots) with a consolidated manifest. Security/privacy scans clean (no keys-as-text, no disk paths, no raw errors, no fake URLs/prod domains). **Decision: PUBLIC CATALOG PRODUCTION-READY WITH MINOR POLISH** — zero must-fix items; deferred polish tracked (gate `/design-system`+`/skeleton` out of prod builds, code-splitting, dedicated a11y audit pass, upload rate-limit, CDN/optimization). *No code changes were needed — gate + evidence + docs only.*
  - **Public interest / lead-capture boundary — complete (C4N).** The first pre-commerce funnel: the public course + learning-path detail pages' disabled "coming soon" CTAs become an honest **Register interest** action opening an accessible bilingual modal (`InterestDialog`) — parent name + phone required, email/child-age/notes optional, `preferredLanguage` inferred from locale, with loading/success/safe-error states and honest copy ("interest received", "enrollment isn't open yet; not a booking or payment"). Backend: a focused `CatalogInterestLead` entity (contact data only — no passwords/tokens/payment) behind `POST /api/v1/catalog/interest` (unauthenticated, rate-limited `catalog-interest` 10/10 min/IP, FluentValidation, source confirmed **published** or 404, minimal `{id,status,createdAt}` response that never echoes contact data) plus staff-only review `GET/GET{id}/PATCH{id}/status` under `CoursePolicies.Manage` (new→contacted→archived). A minimal staff **Interest leads** page (`/staff/catalog/interests`, Catalog nav) lists leads with a status filter + Mark-contacted/Archive/Reopen — no deletion/export/CRM. It grants **no access, enrollment, subscription, or payment** (none exist). **Migration** `20260711125941_AddCatalogInterestLeads` created (adds only the one table; snapshot consistent; **not applied** to any real DB — SQLite tests use EnsureCreated, the disposable LocalDB E2E DB migrates itself). Tests: **+9 backend** (submit course/path, published-unlisted allowed, draft/unknown→404, input validation, admin list/status, 401/403, one-lead-row-only), **+5 frontend** (`InterestDialog`: honest copy, required-field validation before any call, success + correct body, safe API-error, Arabic/RTL) + two detail-view CTA tests updated. E2E `35/35` (course + path detail → open form → intercepted submit → success, still no checkout/payment links); media regressions green. Visual QA: **14 git-ignored screenshots** (7 desktop-LTR + 7 mobile-RTL). Report: [`docs/release-readiness/C4N_PUBLIC_ENROLLMENT_INTEREST_LEAD_CAPTURE.md`](release-readiness/C4N_PUBLIC_ENROLLMENT_INTEREST_LEAD_CAPTURE.md). *No payment/checkout/subscription/access, no lesson player, no student/parent dashboard, no tracking pixels; no contact data exposed publicly; no path leakage.*
  - **Interest-lead admin workflow polish + safe export — complete (C4O).** Frontend-only polish of `/staff/catalog/interests`: URL-bound status filter + **pagination** (fixing leads past page 1 being unreachable via the shared `CatalogPagination`), an editable staff-only **admin note** per lead (display + inline edit, wired to the existing `UpdateStatus` `adminNotes` field — no backend/contract change), and a **safe CSV export** of the current filtered page — client-side only (no bulk server endpoint), RFC-4180 quoting + **spreadsheet formula-injection guard** (leading `= + - @` prefixed; `+`-phone is the trigger) + UTF-8 BOM for Arabic. Extracted a presentational `InterestLeadsView` + dev `?state=` fixtures; en/ar i18n + Visual QA matrix rows. Verified: lint/format/typecheck/build clean, **406** frontend unit, E2E **35/35**, **9** git-ignored screenshots. *No backend changes, no migrations, no bulk-export API, no deletion/CRM.*
  - **Public catalog + interest-leads handoff / merge-readiness pack — complete (C4P).** A handoff/verification/merge-readiness gate over the whole C-series (C1B–C4O, 44 commits), producing [`docs/release-readiness/C4P_PUBLIC_CATALOG_INTEREST_HANDOFF_MERGE_READINESS.md`](release-readiness/C4P_PUBLIC_CATALOG_INTEREST_HANDOFF_MERGE_READINESS.md): branch/base state (44 ahead of `main`, clean fast-forward), commit/feature/route/endpoint inventories, migration + config/env inventories, security/privacy + media + interest + SQL-concurrency reviews, and deployment/migration/rollback/merge checklists. Verification this session (HEAD `ad5a69c`): frontend lint/format/typecheck/build clean, **406** unit, E2E **35/35**; **backend `dotnet build`/`test` + media/media-upload E2E were blocked** — this host has .NET SDK 9 only while `global.json` pins SDK 8 (`global.json` left unchanged) — so they rest on the last accepted green reports and must be re-run on an SDK-8 host/CI. **Decision: READY FOR MERGE REVIEW WITH NOTES** (the note being that SDK-blocked suites need CI re-verification; no code-level must-fix found). *Docs-only; no product code, backend behavior, migration, or artifact changed or applied.*
  - **Final merge conflict dry-run + `main` integration plan — complete (C4Q).** A non-destructive merge dry-run against `main`, producing [`docs/release-readiness/C4Q_FINAL_MERGE_CONFLICT_DRY_RUN_MAIN_INTEGRATION_PLAN.md`](release-readiness/C4Q_FINAL_MERGE_CONFLICT_DRY_RUN_MAIN_INTEGRATION_PLAN.md). `main` is unchanged (`7b8de94`) and a **strict ancestor of HEAD** (`0` behind / **45** ahead), so a fast-forward is possible. Two independent dry-runs — `git merge-tree --write-tree main HEAD` (exit 0, clean tree, no markers) and a temp-branch `git merge --no-commit --no-ff` (**"Automatic merge went well"**, 0 unmerged files, `diff --check` clean, 335 files/+47,297/−174, the 4 C-series migrations, no `api.d.ts`/artifacts) — both report **no conflicts**; the merge was aborted, the branch returned to `280a4cf`, and the temp branch safely removed. Verified this session: frontend lint/format/typecheck/build clean, **406** unit, E2E **35/35**; backend + media E2E **still SDK-8-blocked** (only `9.0.305` installed; `global.json` unchanged). **Decision: READY FOR MAIN MERGE AFTER CI** (the CI gate being the SDK-8 backend/media run). *No merge, push, rebase, reset, migration, or runtime change performed.*
  - **Full CI on SDK-8 host / backend + media verification — complete, green (C4R).** Attempts 1–2 (2026-07-16/17) were blocked because the default `dotnet` on `PATH` resolved to the SDK-9-only install; on **2026-07-21** the gate ran to completion on a genuine SDK 8 toolchain (`8.0.422`, satisfying the `8.0.420` pin — same `8.0.4xx` feature band, higher patch — with **no `global.json` change and no pin bypass**; a compatible SDK 8 was already installed under the user profile, so no new download was needed). Results at HEAD `0b4b7ae`: backend build **0/0**, `dotnet test` **411 passed / 0 failed / 0 skipped** (incl. the 5 LocalDB `SqlServerCatalogUpdateConcurrencyTests`); frontend lint/format/typecheck/build clean + **406** unit; E2E **35/35**; media E2E **12/12** (12 screenshots); media-upload full **Option A** green (7 screenshots, real UI login→upload→preview→save→public hero, C4J fix exercised on real SQL Server, `consoleErrors: []`, no path leak). Safety clean; docs-only; working tree untouched; `main` still `7b8de94` (**0 behind / 48 ahead**). Report: [`docs/release-readiness/C4R_FULL_CI_SDK8_BACKEND_MEDIA_VERIFICATION.md`](release-readiness/C4R_FULL_CI_SDK8_BACKEND_MEDIA_VERIFICATION.md). **Decision: CI VERIFIED — READY FOR MAIN MERGE.** *No SDK bypass, no merge, no migration, no runtime change.*
  - **Local `main` fast-forward merge — complete (C4S).** On **2026-07-21**, after the C4R green gate, local `main` was fast-forwarded `7b8de94 → f5a609a` via `git merge --ff-only claude/c4j-sqlserver-concurrency` (`Updating 7b8de94..f5a609a` → `Fast-forward`; no merge commit, no conflicts, single-parent HEAD). Pre-merge tip backed up at `backup/main-before-c4s` (`7b8de94`). Post-merge on `main`: worktree clean, `main` = feature (0/0), `diff --check` clean; smoke re-run green (lint/format/typecheck/build clean, **406** unit). Report: [`docs/release-readiness/C4S_MAIN_MERGE_COMPLETION.md`](release-readiness/C4S_MAIN_MERGE_COMPLETION.md). **Nothing pushed** — remote unchanged. *No push, rebase, reset, revert, branch delete, migration, or runtime change.*
  - **Remote push / deployment preparation approval — complete (C4T).** Read-only readiness assessment on **2026-07-21**: local `main` (`089a31c`) is clean, linear, CI-verified (C4R) and merge-complete (C4S), but **no git remote is configured**, so no push is possible without a user-provided/approved URL. Grounded checks: health endpoints `/health/live` + `/health/ready` present (`Program.cs:177-178`), CORS/HSTS/seed gates wired, `appsettings.json` carries no real secrets (JWT key is a marked `CHANGE-IN-PROD` dev placeholder, empty connection string, `localhost` CORS). Open pre-prod code fix flagged (not done here): `/design-system` + `/skeleton` are still registered unconditionally in `router.tsx:98-99` and must be gated out of prod. Report: [`docs/release-readiness/C4T_REMOTE_PUSH_DEPLOYMENT_APPROVAL.md`](release-readiness/C4T_REMOTE_PUSH_DEPLOYMENT_APPROVAL.md). **Decision: READY FOR REMOTE CONFIGURATION.** Nothing pushed; no remote added. *No push, remote add/change, force-push, rebase, reset, revert, branch delete, migration, or runtime change.*
  - **Remaining:** **C4U — add remote and push `main`** after the user provides/approves the repository URL (`git remote add origin <URL>` → `git push -u origin main`, pushing commit `089a31c`; no force), or first **C4U — staging deployment runbook execution**; then the **deployment runbook**, the **CDN / object-storage provider** decision (deployment-triggered; the three media store interfaces are ready), and gating `/design-system` + `/skeleton` out of the prod build. Enrollment / payments / lesson player, a media-management/library UI, and an instructor-name public field remain out of scope — do not start payment/enrollment until the interest boundary is reviewed and accepted.

## Phase D — Learning loop

- **D1 — Free enrollment**
- **D2 — Lesson player (recorded content, resume, progress)**
- **D3 — Parent progress dashboard v1 (plain-language)**

## Phase E — Engagement

- **E1 — Coding practice (browser iframe sandbox, no backend execution)**
- **E2 — Assignments + submissions + instructor grading**
- **E3 — Gamification v1 (XP, badges, streaks; reward learning, not session length)**

## Phase F — Commerce

- **F1 — Payment integration (parent-initiated paid enrollment, refunds, webhooks)**

## Phase G — Live & operations

- **G1 — Live sessions (schedule + join via provider adapter; ADR for provider choice)**
- **G2 — Admin analytics + audit log viewer**

## Phase H — Trust & polish

- **H1 — Notifications (email + in-app)**
- **H2 — Certificates (downloadable on course completion)**
- **H3 — COPPA / GDPR data tools (export + delete)**

> The current roadmap is intentionally complete-on-paper but executed task-by-task with its own analysis/plan/approval cycle. We don't open more than one task at a time.
