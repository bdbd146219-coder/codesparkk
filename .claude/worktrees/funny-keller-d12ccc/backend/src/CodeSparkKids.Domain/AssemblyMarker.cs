namespace CodeSparkKids.Domain;

/// <summary>
/// Marker type so other assemblies can reference the Domain assembly
/// (e.g. for assembly scanning) without depending on a concrete entity.
/// </summary>
public static class AssemblyMarker;
