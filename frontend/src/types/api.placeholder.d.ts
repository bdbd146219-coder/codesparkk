/**
 * Placeholder. The real `api.d.ts` is generated from the backend's OpenAPI
 * schema by `npm run gen:api-types` and is git-ignored. Until a feature
 * endpoint lands in Phase B, this file exists only to mark where generated
 * types will live.
 */
export {};
